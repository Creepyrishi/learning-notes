Atomic token-bucket rate limiter.

redis is single threaded

Redis is a in memory stroe of key value paris. they are mostly used to reduce read pressure and 1. Caching 2. ratelimiting 3. background jobs 4. otp and session strore.

there are diffrent type of data structures we can store in the memory in redis some of most common are string, list, hash, geospatial, etc.

